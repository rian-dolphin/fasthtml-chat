[project]
name = "fasthtml-chat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rian-dolphin", email = "rdolphin98@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pydantic-ai>=1.0.6",
    "python-dotenv>=1.1.1",
    "python-fasthtml>=0.12.27",
]

[project.scripts]
fasthtml-chat = "fasthtml_chat:main"

[build-system]
requires = ["uv_build>=0.8.9,<0.9.0"]
build-backend = "uv_build"
